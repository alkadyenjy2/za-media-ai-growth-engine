import { useEffect, useState } from 'react'
import { Megaphone, Plus } from 'lucide-react'
import { supabase } from '../lib/supabase'
import type { Campaign } from '../types/database'

export function CampaignsPage() { const [campaigns, setCampaigns] = useState<Campaign[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { void (async () => { const { data } = await supabase.from('campaigns').select('*, companies(name)').order('created_at', { ascending: false }); setCampaigns((data as Campaign[]) ?? []); setLoading(false) })() }, []); return <div className="animate-fade-in space-y-6"><div className="flex items-end justify-between"><div><p className="text-sm text-neutral-500">Monitor campaign performance across every channel.</p><div className="mt-3 flex items-center gap-2"><span className="text-2xl font-display font-bold">{campaigns.length}</span><span className="text-sm text-neutral-500">campaigns</span></div></div><button className="btn-primary"><Plus size={17}/> New campaign</button></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{loading ? <div className="card p-8 text-sm text-neutral-400">Loading campaigns...</div> : campaigns.map((campaign) => <div className="card p-5" key={campaign.id}><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary-50 text-primary-600"><Megaphone size={18}/></div><span className={`badge-${campaign.status === 'active' ? 'success' : campaign.status === 'paused' ? 'warning' : 'neutral'}`}>{campaign.status}</span></div><h3 className="mt-4 font-display text-lg">{campaign.name}</h3><p className="mt-1 text-sm text-neutral-500">{campaign.companies?.name ?? 'Unassigned'} · {campaign.objective}</p><div className="mt-5 grid grid-cols-3 gap-3 border-t border-neutral-100 pt-4"><div><div className="text-lg font-bold">{campaign.leads_count}</div><div className="text-[11px] text-neutral-500">Leads</div></div><div><div className="text-lg font-bold">${Number(campaign.spend).toLocaleString()}</div><div className="text-[11px] text-neutral-500">Spend</div></div><div><div className="text-lg font-bold">${Number(campaign.budget).toLocaleString()}</div><div className="text-[11px] text-neutral-500">Budget</div></div></div></div>)}{!loading && campaigns.length === 0 && <div className="card col-span-full flex min-h-[320px] flex-col items-center justify-center text-center"><Megaphone className="mb-3 text-neutral-300" size={32}/><h3 className="font-display text-lg">No campaigns yet</h3><p className="mt-1 text-sm text-neutral-500">Campaign performance will appear here once connected.</p></div>}</div></div> }
