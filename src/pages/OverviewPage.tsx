import { useEffect, useState } from 'react'
import { ArrowUpRight, CircleDollarSign, MessageSquare, Target, Users } from 'lucide-react'
import { supabase } from '../lib/supabase'
import type { Lead } from '../types/database'

const statConfig = [{ key: 'leads', label: 'Total leads', icon: Users, color: 'bg-primary-50 text-primary-600' }, { key: 'qualified', label: 'Qualified leads', icon: Target, color: 'bg-accent-50 text-accent-600' }, { key: 'conversations', label: 'Open conversations', icon: MessageSquare, color: 'bg-warning-50 text-warning-600' }, { key: 'revenue', label: 'Paid revenue', icon: CircleDollarSign, color: 'bg-neutral-100 text-neutral-700' }]
export function OverviewPage() { const [leads, setLeads] = useState<Lead[]>([]); const [conversationCount, setConversationCount] = useState(0); const [revenue, setRevenue] = useState(0); const [loading, setLoading] = useState(true); useEffect(() => { void (async () => { const [leadRes, conversationRes, incomeRes] = await Promise.all([supabase.from('leads').select('*').order('created_at', { ascending: false }), supabase.from('conversations').select('id', { count: 'exact', head: true }).eq('status', 'open'), supabase.from('income_records').select('amount').eq('status', 'paid')]); setLeads((leadRes.data as Lead[]) ?? []); setConversationCount(conversationRes.count ?? 0); setRevenue((incomeRes.data ?? []).reduce((sum, item) => sum + Number(item.amount), 0)); setLoading(false) })() }, []); const qualified = leads.filter((lead) => ['qualified', 'appointment', 'won'].includes(lead.status)).length; const values: Record<string, string> = { leads: String(leads.length), qualified: String(qualified), conversations: String(conversationCount), revenue: `$${revenue.toLocaleString()}` }; const pipeline = ['new', 'contacted', 'qualified', 'appointment', 'won', 'lost'].map((status) => ({ status, count: leads.filter((lead) => lead.status === status).length })); return <div className="animate-fade-in space-y-7"><p className="text-sm text-neutral-500">A clear view of your growth engine, updated from live data.</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{statConfig.map(({ key, label, icon: Icon, color }) => <div className="card p-5" key={key}><div className="flex items-start justify-between"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${color}`}><Icon size={19}/></div><ArrowUpRight size={16} className="text-neutral-300"/></div><div className="mt-5 text-2xl font-display font-bold">{loading ? '—' : values[key]}</div><div className="mt-1 text-sm text-neutral-500">{label}</div></div>)}</div><div className="grid gap-6 xl:grid-cols-5"><div className="card p-6 xl:col-span-3"><div className="flex items-center justify-between"><div><h2 className="text-lg">Pipeline health</h2><p className="mt-1 text-sm text-neutral-500">Lead distribution by stage</p></div><span className="badge-success">Live</span></div><div className="mt-7 space-y-4">{pipeline.map(({ status, count }) => <div key={status}><div className="mb-1.5 flex justify-between text-sm"><span className="capitalize text-neutral-600">{status}</span><span className="font-semibold text-neutral-800">{count}</span></div><div className="h-2 overflow-hidden rounded-full bg-neutral-100"><div className={`h-full rounded-full ${status === 'won' ? 'bg-accent-500' : status === 'lost' ? 'bg-error-400' : 'bg-primary-500'}`} style={{ width: `${leads.length ? Math.max((count / leads.length) * 100, count ? 4 : 0) : 0}%` }}/></div></div>)}</div></div><div className="card p-6 xl:col-span-2"><h2 className="text-lg">Recent leads</h2><p className="mt-1 text-sm text-neutral-500">Latest opportunities entering the system</p><div className="mt-5 divide-y divide-neutral-100">{leads.slice(0, 5).map((lead) => <div key={lead.id} className="flex items-center justify-between py-3"><div><div className="text-sm font-semibold text-neutral-800">{lead.full_name}</div><div className="text-xs text-neutral-500">{lead.source} · {new Date(lead.created_at).toLocaleDateString()}</div></div><span className="text-sm font-bold text-neutral-700">${Number(lead.estimated_value).toLocaleString()}</span></div>)}{!loading && leads.length === 0 && <div className="py-8 text-center text-sm text-neutral-400">Your recent leads will appear here.</div>}</div></div></div></div> }
